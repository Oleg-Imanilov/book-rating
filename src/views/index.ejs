<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Books Rating</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <main class="container">
      <%- include("partials/header", {
        title: "Books Rating",
        nav: { books: true, summary: true, me: true, login: true, register: true, logout: true, authStatus: true }
      }) %>

      <section class="card">
        <h2>Quick test checklist</h2>
        <ol class="steps">
          <li>Register a user.</li>
          <li>Login and confirm status below.</li>
          <li>Open Books and add a title.</li>
          <li>Open My Top 10 and add a ranked entry.</li>
          <li>Logout and confirm status returns to Anonymous.</li>
        </ol>
        <div class="status">
          <span>Status: <strong><%= currentUser ? "Authenticated" : "Anonymous" %></strong></span>
          <% if (currentUser) { %>
          <span>User ID: <strong><%= currentUser.id %></strong></span>
          <% } %>
        </div>
      </section>
    </main>
  </body>
</html>
